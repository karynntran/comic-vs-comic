<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username, :placeholder => "email..." %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.password_field :password, :placeholder => "password..." %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.password_field :image, :placeholder => "image..." %>
  </div>
  <div class="field">
    <%= f.label :powers %><br>
    <%= f.password_field :powers, :placeholder => "your special abilities..." %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
